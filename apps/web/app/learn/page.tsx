import { PageContainer } from "@/components/shared/PageContainer";
import { SectionHeader } from "@/components/shared/SectionHeader";

export default function LearnPage() {
  return (
    <PageContainer>
      <SectionHeader
        eyebrow="Learn"
        title="Learning Engine — coming next"
        description="Intent → Offer → Outcome learning will land in a later stage."
      />
    </PageContainer>
  );
}
