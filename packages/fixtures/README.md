# Fixtures

Stage 1 synthetic merchant data is generated by:

```bash
make seed
```

The generator lives in `apps/api/app/seed/` and upserts a deterministic
Astra Electronics headphone catalogue using `ASTRAOS_SEED=2026`.
